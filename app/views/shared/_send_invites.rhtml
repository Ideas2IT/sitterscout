<%= flash[:notice] %>
<div style="margin-left: 15px;">
Thank you for inviting your friends to join SitterScout!<br /><br />
<% unless @names.blank? %>
You have invited the following people:<br>
<br>
<div style="font-size: .8em;">
<%= @names.join("<br />") %> 
</div>
<% end %>
<% unless @users.size < 1 %>

The following people are already part of the SitterScout Community<br>
<table style="font-size: .8em;">
<tr>
		<td>&nbsp;</td>
</tr>
<% @users.each do |user| %>
	<% unless user.profile.nil? %>
	<tr>
		<td><%= user.profile.first_name %>&nbsp;<%= user.profile.last_name %></td>
		<td style="padding-left: 50px"><%= user.profile.parent? ? (link_to "Add to Your Friends", url_for(:controller => 'connections', :action => 'add_friend', :id => user.id), :class => "lbOn-300-260") : (link_to "Add to Your Sitters", url_for(:controller => 'connections', :action => 'add_friend', :id => user.id), :class => "lbOn-300-260")  %></td>
	</tr>
	<% end %>
<% end %>
</table>
<% end %>
</div>
<br />

